spring:
  datasource:
    url: jdbc:h2:mem:db
    driver-class-name: org.h2.Driver
    username: test
    password: ""

  jpa:
    show-sql: true

  h2:
    console:
      enabled: true
      path: /testDB